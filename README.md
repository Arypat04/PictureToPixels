# PictureToPixels
Program takes an image file inputted into the command prompt and changes that images content into a different image comprised of red, green and blue pixels to show the same image inputted
